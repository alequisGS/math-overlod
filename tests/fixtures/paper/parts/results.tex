% Fake structure: \begin{theorem}\label{not-real}\end{theorem}
\begin{theorem}[A {nested} name]\label{thm:stable}
The canonical stack of $\Xten$ admits an exceptional collection.
See \cref{def:fixture,sec:geometry} and \cite[Chapter {2}]{Source}.
\begin{equation}\label{eq:fixture}x=1\end{equation}
\end{theorem}
% Comments between statement and proof are harmless.
\begin{proof}
\begin{enumerate}\item First step.\end{enumerate}
Use \Cref{lem:fixture} and \eqref{eq:fixture}; \textcite{Source}.
\end{proof}
\input{lemma}
